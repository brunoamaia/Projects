// Insert the value of the number pressioned
// 

function digit (numb, inf) {
    console.log(`\nvalor = ${numb} txt = ${inf}`)
    let val = num.toString(numb)
    let txt = inf
    switch (val) {
        case 0:
            val += 0
            console.log(`val = ${val}`);
            return txt = val
        case 1:
            val += 1
            console.log(`val = ${val}`);
            return txt = val
        case 2:
            dis += 2
            display.innerHTML = `${txt}`
            return txt = dis
        case 3:
            dis += 3
            display.innerHTML = `${txt}`
            return txt = dis
        case 4:
            dis += 4
            display.innerHTML = `${txt}`
            return txt = dis
        case 5:
            dis += 5
            display.innerHTML = `${txt}`
            return txt = dis
        case 6:
            dis += 6
            display.innerHTML = `${txt}`
            return txt = dis
        case 7:
            dis += 7
            display.innerHTML = `${txt}`
            return txt = dis
        case 8:
            dis += 8
            display.innerHTML = `${txt}`
            return txt = dis
        case 9:
            dis += 9
            display.innerHTML = `${txt}`
            return txt = dis
    }
}

export { digit }

/*
const value = function (cond) {
    console.log(`chamou modulo`);
    let val = Number(cond)
    switch (val) {
        case 0:
            txt += 0
            display.innerHTML = `${txt}`
            break;
        case 1:
            txt += 1
            display.innerHTML = `${txt}`
            break;
        case 2:
            txt += 2
            display.innerHTML = `${txt}`
            break;
        case 3:
            txt += 3
            display.innerHTML = `${txt}`
            break;
        case 4:
            txt += 4
            display.innerHTML = `${txt}`
            break;
        case 5:
            txt += 5
            display.innerHTML = `${txt}`
            break;
        case 6:
            txt += 6
            display.innerHTML = `${txt}`
            break;
        case 7:
            txt += 7
            display.innerHTML = `${txt}`
            break;
        case 8:
            txt += 8
            display.innerHTML = `${txt}`
            break;
        case 9:
            txt += 9
            display.innerHTML = `${txt}`
            break;
    }
}

export  { 
    value
} */